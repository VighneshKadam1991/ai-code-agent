spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/codeagent
    username: postgres
    password: postgres

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

claude:
  model: claude-sonnet-4-20250514
