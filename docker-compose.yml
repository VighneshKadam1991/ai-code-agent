version: '3.9'
services:
  postgres:
    image: postgres:16
    container_name: ai-code-agent-db
    environment:
      POSTGRES_DB: codeagent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
